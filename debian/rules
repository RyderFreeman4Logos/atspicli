#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	cargo build --release --locked

override_dh_auto_test:
	cargo test --locked

override_dh_auto_install:
	install -D -m 0755 target/release/atspicli $(CURDIR)/debian/atspicli/usr/bin/atspicli
